# translate-file skill Python dependencies
pypandoc>=1.13
PyMuPDF>=1.23  # PDF fallback when calibre is unavailable
python-docx>=1.1  # DOCX native in-place translation pipeline
pdf2docx>=0.5.8   # PDF→DOCX bridge (layout-preserving PDF conversion)
openpyxl>=3.1     # XLSX native translation pipeline
python-pptx>=0.6.23   # PPTX native translation pipeline
chardet>=5.2           # CSV encoding detection (UTF-8/latin-1/Windows-1252)
python-frontmatter>=1.0  # Markdown front matter (YAML/TOML) strip + restore
Pillow>=10.0           # Image text overlay for JPG/PNG translation
numpy>=1.24            # Array ops for image rendering (KMeans color sampling)
scikit-learn>=1.3      # KMeans bg/fg color detection for image translation
