# Component Catalog

<!-- authored by rebuild-spec system-researcher from .system-scout-report.md + component docs -->
<!-- snapshot-hash: {{FILL: copy the snapshot-hash value from .system-scout-report.md}} -->

{{FILL: BUILD a Markdown table | Service | Role | Stack | Domain / Entities | Contracts Exposed |
Dependencies | Confidence | Module | Responsibility |, one row per service. Pull role/stack/entities/
contracts/dependencies/confidence from the scout report; Module is a link `../components/<name>/`. The
Responsibility cell is authored prose from READING the component's docs — a reused component is read,
never summarized from its thin digest. Never leave a row thin because a component is reused.}}

## Notes

{{FILL: catalog caveats — components with low extraction confidence; reused/docs-derived components and
what their docs ADDED beyond the digest (cite the docs you read); excluded components with dangling refs.}}
