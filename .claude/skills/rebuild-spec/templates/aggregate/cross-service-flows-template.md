# Cross-Service Flows

<!-- authored by rebuild-spec system-researcher from .system-scout-report.md + component docs -->
<!-- snapshot-hash: {{FILL: copy the snapshot-hash value from .system-scout-report.md}} -->

> **[UNVERIFIED]** — saga handoffs are heuristic; confirm with a human.

## Sagas

{{FILL: DRAW a Mermaid `sequenceDiagram` covering the cross-service handoffs — the scout report's edges
PLUS any saga you found by READING the components' docs (e.g. a Kafka request/reply loop whose consumer
the thin digest missed). Safe participant labels. Then name each saga and describe it: trigger, ordered
handoffs, failure handling. Cite the component doc for each docs-derived step and keep it [UNVERIFIED].
Only write "_(no cross-service sagas detected)_" if the docs genuinely show none.}}
