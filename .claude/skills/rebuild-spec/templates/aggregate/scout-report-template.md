<!-- FORMAT SPEC — Python (synthesize_system.py --aggregate) GENERATES this file at
     docs/<primary>/system/.system-scout-report.md. It is the system-researcher's FACTS INPUT — read,
     never authored by hand, never embedded verbatim in the final docs (v19: Python emits DATA only;
     the LLM authors all tables + Mermaid from these facts + the components' docs). All string fields
     are sanitize_field'd. NO Mermaid here — diagrams are the researcher's to draw. -->
# System Scout Report (facts — do not edit)

<!-- generated by rebuild-spec synthesize_system — {timestamp} -->
### snapshot-hash
`{snap}`

## Services
<!-- per component: name, role, stack, reused flag, ABSOLUTE docs path (so the researcher knows what to open) -->
| Service | Role | Stack | Reused | Docs path |
|---------|------|-------|--------|-----------|

## Edges (statically observed — [UNVERIFIED])
| From | To | Type | Label | Status |
|------|----|------|-------|--------|

## Fan-in / Fan-out
| Service | Fan-out (calls) | Fan-in (called by) |
|---------|-----------------|--------------------|

## Self-loop Topics
<!-- list, or "(none)" -->

## Entity Ownership
| Entity | Owner Service | ID Field | ID Type | Visibility |
|--------|---------------|----------|---------|------------|

## Cross-Service Correlation Suggestions
| Entity A (Service) | Entity B (Service) | Match Reason | Status |
|--------------------|--------------------|--------------|--------|

## Event Flows (producer → consumers)
| Topic | Producer(s) | Consumer(s) |
|-------|-------------|-------------|

## Per-component Confidence
| Service | Confidence | Signals |
|---------|------------|---------|
