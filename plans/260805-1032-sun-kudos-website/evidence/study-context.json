{
  "task": "Phase-02 Sun* Kudos — toàn bộ schema Postgres: 12 bảng, 2 view bảo mật, 5 trigger, 7 index, RLS + khối revoke tường minh, seed demo. Phase mang nhiều rủi ro bảo mật nhất của plan: quyết định che danh tính người gửi ẩn danh ở tầng DB (không ở UI) nằm ở đây.",
  "mode": "code",
  "acceptanceCriteria": [
    "npx supabase db reset exit 0, không warning về bảng thiếu RLS",
    "select count(*) from badges = 6 và sum(probability_weight) = 100",
    "select count(*) from departments = 50; hashtags = 13",
    "select count(*) from pg_tables where tablename = 'kudos_mentions' = 0",
    "Ghi trực tiếp bị chặn: insert kudos / insert kudos_hashtags / insert hearts / delete hearts đều lỗi quyền với phiên authenticated",
    "pg_proc: mọi hàm security definer có search_path=public, pg_temp",
    "admin_grant_secret_box(): user thường FORBIDDEN; admin tăng đúng số hàng unopened",
    "my_sent_kudos: phiên A trả đúng kudos A gửi; phiên B không thấy hàng nào của A",
    "public_kudos_feed: sender_id và sender_full_name đều NULL với kudos ẩn danh",
    "CHECK kudos_no_self chặn self-kudos; UNIQUE chặn tim trùng",
    "Tim ngày đặc biệt +2; xoá tim đó thu hồi đúng 2",
    "database.types.ts có type cho cả 12 bảng và 2 view"
  ],
  "touchpoints": [
    "supabase/migrations/0001_core_master_tables.sql",
    "supabase/migrations/0002_profiles_and_roles.sql",
    "supabase/migrations/0003_kudos_tables.sql",
    "supabase/migrations/0004_secret_box_tables.sql",
    "supabase/migrations/0005_triggers_and_indexes.sql",
    "supabase/migrations/00061_realtime_broadcast_triggers.sql",
    "supabase/migrations/0006_views_and_rls.sql",
    "supabase/seed.sql",
    "scripts/count-departments.mjs",
    "lib/supabase/database.types.ts"
  ],
  "blastRadius": "Schema là nền của mọi phase còn lại. Sai RLS/grant ở đây là lỗ hổng bảo mật trên toàn site, không phải lỗi một màn. Hai view là ranh giới ẩn danh và ranh giới sent-list — mọi màn đọc kudos đều đi qua chúng. database.types.ts sinh lại ảnh hưởng typecheck toàn repo.",
  "contracts": [
    "public_kudos_feed: ranh giới ẩn danh, grant anon+authenticated — mọi màn đọc feed dùng view này, KHÔNG chạm bảng kudos",
    "my_sent_kudos: definer view, where sender_id = auth.uid(), chỉ grant authenticated",
    "Bảng kudos: revoke toàn bộ với anon/authenticated — không đọc, không ghi trực tiếp",
    "kudos_hashtags/kudos_images/hearts: chỉ SELECT; mọi ghi qua RPC phase-04",
    "profiles: column-level grant, KHÔNG có sent_kudos_count — select('*') sẽ lỗi 42501",
    "Realtime dùng Broadcast (topic kudos-board, user-hearts:<id>), payload chỉ id — KHÔNG Postgres Changes",
    "admin_grant_secret_box(uuid[], int) — đường cấp Secret Box giữa sự kiện"
  ]
}